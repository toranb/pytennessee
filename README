A simple ember.js and django-rest-framework web application


To run the web application

    npm install
    mkvirtualenv webapp
    pip install -r requirements.txt
    python manage.py runserver

    visit http://localhost:8000 to see the running app
